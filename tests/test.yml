---
- name: generic tests for all the roles
  hosts: all

  vars:
    ansible_python_interpreter: /usr/bin/python3

  roles:
    - ../roles/changedetection
    - ../roles/homer
    - ../roles/matomo
    - ../roles/nodered
    - ../roles/vaultwarden
